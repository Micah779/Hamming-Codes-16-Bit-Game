import javax.swing.JFrame;

class CodesMain {
    public static void main(String[] args) { // creates a frame object which contains the game
        // Create the frame
        CodesFrame frame = new CodesFrame();
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.setVisible(true);
    }
}